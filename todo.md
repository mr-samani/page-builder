# در ویژگی های display flex مانند gap px وجود ندارد

# window in SSR in preview

# set internal link without reload page for SPA => routerLink for inner links

# url not set correctly in link => must be replace dynamic data
