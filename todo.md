# وقتی تگ span توی collection کردم b همه تغییر نکردن

# در typography font-weight نداریم

# در ItemCollection باید collection container را بتوان تغییر داد

# در ویژگی های display flex مانند gap px وجود ندارد

# window in SSR in preview
