import { InjectionToken } from '@angular/core';
import { IPageBuilderHtmlEditor } from './IHtmlEditor';

export const NGX_PAGE_BUILDER_HTML_EDITOR = new InjectionToken<IPageBuilderHtmlEditor>(
  'NGX_PAGE_BUILDER_HTML_EDITOR',
);
