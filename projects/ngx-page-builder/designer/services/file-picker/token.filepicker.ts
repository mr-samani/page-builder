import { InjectionToken } from '@angular/core';
import { IPageBuilderFilePicker } from './IFilePicker';

export const NGX_PAGE_BUILDER_FILE_PICKER = new InjectionToken<IPageBuilderFilePicker>(
  'NGX_PAGE_BUILDER_FILE_PICKER',
);
